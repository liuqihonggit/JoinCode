name: CI TUI Daily

on:
  workflow_dispatch:
  schedule:
    - cron: '20 8 * * *'

concurrency:
  group: tui-daily
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPT_OUT: 1
  DOTNET_NOLOGO: true
  NUGET_XMLDOC_MODE: skip
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages
  Configuration: Release

jobs:
  tui-tests:
    name: TUI Unit Tests
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: 'global.json'

      - name: Cache NuGet Packages
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj', 'Directory.Build.props') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Build
        run: dotnet build test/unit/tui.tests/Tui.Tests.csproj -c Release --nologo

      - name: Test
        run: >
          dotnet test test/unit/tui.tests/Tui.Tests.csproj
          -c Release --no-build --nologo
          --filter "Category!=Integration&Category!=Benchmark"
          --logger "console;verbosity=detailed"
          --logger "trx;LogFileName=Tui.trx"

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: tui-test-results
          path: '**/Tui.trx'
          retention-days: 7

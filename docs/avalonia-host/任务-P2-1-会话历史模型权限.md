# 任务：P2-1 GUI 会话历史/模型切换/权限确认

## 背景
P1 已完成（JccChatSession 引擎组装 + MainViewModel 流式对话）。侧边栏会话目前仅内存中自动命名，模型下拉硬编码 3 项，权限确认缺失。本任务补齐三项真实能力。

## 子任务
| # | 子任务 | 现状 | 目标 |
|---|--------|------|------|
| A | 会话历史持久化 | 侧边栏仅内存 | 用 IChatService 历史填充 + 重启恢复 |
| B | 模型切换真实化 | 硬编码 ["gpt-4o","deepseek-chat","claude-sonnet"] | 从配置/Provider 定义读取真实模型列表 |
| C | 权限确认 UI | 无 | 引擎权限请求 → GUI 确认弹窗闭环 |

## 研究结论

### B 模型切换真实化（用户明确：绑定同一个配置源，不拆两套）
- 真实模型数据源：`ModelConfigLoader.GetModels(provider)`（Abstractions 层，GUI 可安全引用）
- 当前 MainViewModel 硬编码 `ModelOptions = ["gpt-4o","deepseek-chat","claude-sonnet"]` — 需替换为加载配置的 provider 对应的真实模型
- 选中模型绑定同一 WorkflowConfig.Provider.ModelId，持久化回共享配置，下次启动引擎用它
- 不触碰引擎 QueryService 运行时切换（引擎读 Config.ModelId），避免扩大改动面

## 里程碑记录
（每步：红测试 → 实现 → 编译 → 绿测试 → git 提交）

### B 模型切换真实化 ✅
- 步骤1：`IJccChatSession` 增加 `CurrentProvider` / `CurrentModelId` / `AvailableModels`
- 步骤2：`JccChatSession` 实现绑定共享 `ModelConfigLoader.GetModels(provider)`（同一份 models.json，不拆两套）
- 步骤3：`PlaceholderChatSession` 同步实现（deepseek 固定真实模型）
- 步骤4：`MainViewModel` 移除硬编码 `["gpt-4o","deepseek-chat","claude-sonnet"]`，改为 `_session.AvailableModels`；默认选中 `_session.CurrentModelId`
- 测试：新增 2 个 ModelSurface 测试 + 1 个 VM 绑定测试 = 72 全绿

<!-- 🤖 Auto Decision: 2026-08-08 -->
<!-- 决策: 模型下拉绑定引擎共享 ModelConfigLoader，选中模型从引擎会话读取 -->
<!-- 原因: 用户明确"绑定同一个"，不拆两套工程；避免 GUI 硬编码与 models.json 漂移 -->
<!-- 替代方案: 引擎运行时热切换（QueryService 请求期读 Config.ModelId，需查 FastMode 链路，改动面大，暂缓） -->
<!-- 验证: GUI Debug 编译通过，72 测试全绿 ✅ -->

### B 模型切换真实化-写回 ✅
- 步骤5：`IJccChatSession.SetModelAsync` — 回写共享 `WorkflowConfig.Provider.ModelId`（DI 单例，QueryService 请求期读同一实例，切换即时生效）
- 步骤6：`MainViewModel.OnSelectedModelChanged` 部分方法触发写回
- 测试：73 全绿（+1 写回测试）

<!-- 🤖 Auto Decision: 2026-08-08 -->
<!-- 决策: 模型切换通过门面 SetModelAsync 回写共享 WorkflowConfig 单例 -->
<!-- 原因: ProviderConfig 注册为 DI 单例，QueryService.CreateRequest 每请求读 Config.ModelId，回写同一实例即真切换 -->
<!-- 替代方案: 重建 QueryService / 走 FastMode 链路（改动面大） -->
<!-- 验证: GUI Debug 编译通过，73 测试全绿 ✅ -->

### A 会话历史持久化 ✅
- 步骤1：`GuiSessionStore` — 读写同一 sessions 目录（~/.jcc/sessions/{Id}.json），JSON 形状对齐 CLI SessionData（PascalCase），CLI /resume 与 GUI 共享会话文件；注入 IFileSystem（生产 Physical / 测试 InMemory）
- 步骤2：`GuiJsonContext` — AOT 兼容源码生成 JSON 上下文
- 步骤3：`SessionItem.Id` — 会话唯一 ID（Guid）
- 步骤4：VM 接线 — 启动 LoadPersistedSessions 恢复侧边栏；发送后 SaveActiveSession 持久化（含自动命名标题）；SelectSession 恢复消息；RemoveSession/ClearAllSessions 删持久化文件
- 测试：4 个 Store 测试 + 2 个 VM 持久化测试 = 79 全绿；清理测试污染 ~/.jcc/sessions 的 9 个文件移至 .xxx/session-test-junk

<!-- 🤖 Auto Decision: 2026-08-08 -->
<!-- 决策: GUI 会话持久化到 CLI 同一 sessions 目录同 JSON 形状（绑定同一个，不另建一套） -->
<!-- 原因: 用户要求绑定同一个配置源；CLI 的 /resume 与 GUI 侧边栏可互相看到对方会话 -->
<!-- 替代方案: GUI 独立 sessions 目录（两套数据漂移，弃用）；复用 CLI SessionData 类型（跨工程耦合，弃用） -->
<!-- 验证: GUI Debug+Release 编译通过，79 测试全绿 ✅ -->

### C 权限确认 UI 闭环（进行中）
- 研究：权限链路为 `PermissionCheckingInterceptor.CheckPermissionOrThrowAsync` → 抛 `PermissionPendingConfirmationException`（interactive 模式）→ 经 `ChatToolOrchestrator` 重掷 → 传播出 `StreamWithEventsAsync`；用户消息已在 `ChatPreprocessor` 阶段入上下文；CLI 的 `ShowPermissionConfirmation` 是死代码（引擎历史上从未闭环权限确认）
- 步骤1 ✅：`IToolPermissionManager` 接口补 `ApproveToolTemporarily(toolName, duration)` / `RemoveTemporaryApproval(toolName)`（`PermissionManager` 已有实现，仅接口未暴露）→ Guard 3 测试绿（未批准待确认 / 批准即授权 / 过期恢复待确认）
- 步骤2 ✅（提交 `19124df4b`）：网关重试闭环 — `PermissionConfirmationRequest/Decision` DTO+枚举；`IJccChatSession.PermissionConfirmationHandler`；`JccChatSession` 捕获 `PermissionPendingConfirmationException`→handler 决策→Allow=批准（本次5分钟/始终24小时）+`RewindLastTurn`→重发同消息（`TrimLastTurn` 保证无重复，MaxRetries=3）→ 3 网关测试绿
- 步骤3 ✅（提交 `47a6b96`）：`MainViewModel` 注入 `PermissionConfirmCallback`（View 层弹窗）→ 构造器接线 `_session.PermissionConfirmationHandler`；未注入回调时默认拒绝 → 2 VM 接线测试绿

<!-- 🤖 Auto Decision: 2026-08-08 -->
<!-- 决策: 权限确认采用"扩接口+网关重试"方案（用户选定） -->
<!-- 原因: 引擎侧 PermissionManager 已有 ApproveToolTemporarily 实现仅未暴露；网关捕获异常→弹窗→批准+RewindLastTurn→重发同消息可实现无重复闭环 -->
<!-- 替代方案: 仅弹窗不重试（用户需手动重发，体验差）；仅GUI提示（无闭环） -->
<!-- 验证: Guard.Security.Tests 3 测试绿 ✅ -->

<!-- 🤖 Auto Decision: 2026-08-08 -->
<!-- 决策: MainViewModel 通过公开 PermissionConfirmCallback 委托接线权限确认，由 View 层注入弹窗实现 -->
<!-- 原因: 保持 VM 无 Avalonia 依赖（可单测），弹窗行为归 View；未注入回调时 VM 默认 Deny 保证无弹窗环境行为可预期 -->
<!-- 替代方案: VM 直接弹 Avalonia 对话框（不可单测，破坏分层）；引擎侧自行决策（无 UI 决策权） -->
<!-- 验证: JoinCodeGui.Tests 84 测试全绿（含 3 网关 + 2 VM 接线）✅ -->

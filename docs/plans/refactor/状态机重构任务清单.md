# 状态机重构任务清单

> 计划文档：`docs/plans/状态机重构计划.md`
> 决策确认：① StateMachine 下沉 foundation ② 统一 TaskState/TaskExecutionStatus ③ 熔断器不用 StateMachine ④ 旧 API 直接删除

## 执行顺序（调整后）

### 第一梯队

| 序号 | 候选 | 状态 | 说明 |
|------|------|------|------|
| 1 | C2 `BridgeStatusUtil` | ✅ 完成 | 纯函数，4-bool → BridgeStatusState 枚举 |
| 2 | A2 `PluginFiber` | ✅ 完成 | 需先下沉 StateMachine 到 foundation |
| 3 | B7 `PeerSessionManager` | ✅ 完成 | 3 状态 if 验证,幂等不重复触发 |
| 4 | A3+A4 熔断器统一 | ✅ 完成 | 归档 TransportCircuitBreaker,UnifiedCircuitBreaker 为唯一实现 |
| 5 | A1 `AgentStateMachine` | ✅ 完成 | 迁移到 StateMachine<TaskExecutionStatus> |
| 6 | B1 `LspServerInstance` | ✅ 完成 | 5 状态分散转换集中化 |
| 7 | C4 `WorkSecretEntry` | ✅ 完成 | 2 bool 归纳为 SecretLifecycle 枚举 |
| 0 | StateMachine 下沉 | ✅ 完成 | infrastructure → foundation/Abstractions |
| 8 | 枚举统一 TaskStatus+TaskExecutionStatus | ✅ 完成 | 19文件,添加 Stopped,成员名映射 InProgress→Running/WaitingForDependencies→WaitingForDependency |
| 9 | B6 `TaskRuntime` | ⏭️ 跳过 | 简单CRUD允许任意状态赋值,测试存在 Pending→Completed 直接跳跃,加状态机验证会破坏现有行为 |

### 第二梯队（待第一梯队完成后评估）

| 序号 | 候选 | 说明 |
|------|------|------|
| 10 | B2 `BridgeSessionRunner` | ✅ 完成 | guard 方法集中,不内嵌 StateMachine(JSON 数据模型) |
| 11 | B3 `MonitorMcpTaskExecutor` | ✅ 完成 | 移除 Paused 死状态,显式赋值保持兼容 |
| 12 | C1 `BridgeState` | ✅ 完成 | 9 属性归纳为 BridgeLifecycleState 枚举,移除 7 死属性 |
| 13 | B5 `SystemActuatorRegistry` | ✅ 完成 | guard 方法集中,异步回调转换验证 |
| 14 | C7 `AgentExecutionContext` | ✅ 完成 | bool?+bool 归纳为 AgentOutcome 枚举 |
| 15 | C3 `ChatUiMessage` | ⏭️ 跳过 | 已用 Kind 枚举+计算属性归纳,剩余 bool 是 UI 绑定子状态,XAML 需简单 bool |

### 第三梯队（需充分测试）

| 序号 | 候选 | 说明 |
|------|------|------|
| 16 | B4 `AgentBase` | ✅ 完成 | 移除 _isPaused 冗余 bool,双枚举统一留待枚举统一 |
| 17 | B8 `WorkflowTaskExecutor` | ⏭️ 跳过 | 已用 WorkflowExecutionMode 枚举+switch 归纳三种模式 |
| 18 | C6 `PromptTestConfig` | ⏭️ 跳过 | 10 bool 是独立测试配置开关可自由组合,非互斥状态 |
| 19 | D1 连接状态枚举统一 | ⏭️ 跳过 | 5 枚举语义各异,TransportConnectionState/SshConnectionState 相同但序列化特性不同,统一风险高 |
| 20 | D3 会话状态枚举统一 | ⏭️ 跳过 | 4 枚举语义完全不同(Bridge生命周期/Peer连接/并发忙闲/Code存储),非同一概念 |

---

## 进度日志

<!-- 每完成一个候选在此记录 -->

### 2026-08-23 第一梯队完成

| 候选 | 提交 | 说明 |
|------|------|------|
| StateMachine 下沉 | 1e61f68 | infrastructure → foundation/Abstractions，51 处 global using 自动覆盖 |
| C2 BridgeStatusUtil | fa3d38c | 4-bool → BridgeStatusState 枚举，消除 16 组合仅 5 合法的隐式约束 |
| A2 PluginFiber | dfe0584 | 复用 StateMachine，保持 [INF-FIBER-ILLEGAL] 异常兼容 |
| B7 PeerSessionManager | 096bc78 | if 验证幂等，3 状态不值得 StateMachine（减法思维） |
| A3+A4 熔断器 | 83fe6e7 | 归档 TransportCircuitBreaker，UnifiedCircuitBreaker 为唯一 3 状态实现 |
| A1 AgentStateMachine | 2c9823 | 复用 StateMachine<TaskExecutionStatus>，转换表从 switch 提取 |
| B1 LspServerInstance | cb13eb | 内嵌 StateMachine<LspServerState>，Error 路径 ForceTransitionTo |
| C4 WorkSecretEntry | c6ce36d | SecretLifecycle 计算属性，保留 bool JSON 兼容 |

**收益**：消除 3 处手写状态机重复（A1/A2/A3），5 处分散转换集中验证（B1/B7），2 处多 bool 归纳枚举（C2/C4），1 处基础设施下沉。

### 2026-08-23 第二梯队 B2 完成

| 候选 | 提交 | 说明 |
|------|------|------|
| B2 BridgeSessionRunner | 6db1cc9 | 提取 BridgeSessionTransitions guard 方法,5 方法内联 if 集中,不内嵌 StateMachine(BridgeSession 是 JSON 数据模型,Status 公开 setter) |

<!-- 🤖 Auto Decision: 2026-08-23 -->
<!-- 决策: B2 不内嵌 StateMachine,改为提取静态 guard 方法类 BridgeSessionTransitions -->
<!-- 原因: BridgeSession 是 JSON 序列化数据模型(Status {get;set;} 公开),内嵌 StateMachine 会破坏反序列化;不同动作对同一目标状态 guard 不同(Resume 仅 Suspended→Active,KeepAlive 允许非终态→Active),纯转换表无法区分动作 -->
<!-- 替代方案: 在 Runner 中维护 StateMachine 字典,但需双状态同步,增加复杂度,违背减法思维 -->
<!-- 验证: 编译通过,27 测试全部通过(10 新增+17 现有) ✅ -->

### 2026-08-23 第二梯队完成

| 候选 | 提交 | 说明 |
|------|------|------|
| B2 BridgeSessionRunner | 6db1cc9 | BridgeSessionTransitions guard 方法集中,不内嵌 StateMachine(JSON 数据模型) |
| B3 MonitorMcpTaskExecutor | 6a51ed8 | 移除 Paused 死状态,显式赋值 Stopped=3 Error=4 保持 JSON 数字兼容 |
| C1 BridgeState | 2abb593 | 9 属性归纳为 BridgeLifecycleState 枚举(Disabled/Enabled/Connected),移除 7 死属性 |
| B5 SystemActuatorRegistry | fdbbe51 | BackgroundTaskStateTransitions guard 集中,异步回调转换验证 |
| C7 AgentExecutionContext | 417b9ae | IsSuccess(bool?)+IsCancelled(bool) 归纳为 AgentOutcome 枚举(4 状态) |
| C3 ChatUiMessage | ⏭️ 跳过 | 已用 Kind 枚举+计算属性归纳,剩余 bool 是 XAML 绑定子状态不适合枚举化 |

**收益**：4 处分散状态转换集中 guard（B2/B5），2 处死状态/死属性清理（B3/C1），1 处 bool 归纳枚举（C7），1 处不适用确认（C3）。

<!-- 🤖 Auto Decision: 2026-08-23 -->
<!-- 决策: C3 ChatUiMessage 跳过重构 -->
<!-- 原因: 已用 Kind 枚举+计算属性(IsToolCall/IsThinking 等)完成归纳,剩余独立 bool(IsStreaming/IsToolError/IsToolRunning/IsThinkingExpanded)是各 Kind 的子状态,XAML 绑定需简单 bool 属性控制可见性,改为枚举需 Converter 增加 UI 层复杂度 -->
<!-- 验证: 分析确认,未改动代码 ✅ -->

### 2026-08-23 第三梯队完成

| 候选 | 提交 | 说明 |
|------|------|------|
| B4 AgentBase | fe12ad8 | 移除 _isPaused 冗余 bool,用 Status==Paused 替代,双枚举 Status/State 统一留待枚举统一任务 |
| B8 WorkflowTaskExecutor | ⏭️ 跳过 | 已用 WorkflowExecutionMode 枚举+switch 归纳三种执行模式 |
| C6 PromptTestConfig | ⏭️ 跳过 | 10 bool 是独立测试配置开关可自由组合,非互斥状态 |
| D1 连接状态枚举统一 | ⏭️ 跳过 | 5 枚举语义各异,TransportConnectionState/SshConnectionState 相同但序列化特性不同,统一风险高 |
| D3 会话状态枚举统一 | ⏭️ 跳过 | 4 枚举语义完全不同(Bridge生命周期/Peer连接/并发忙闲/Code存储),非同一概念 |

**收益**：1 处冗余 bool 移除（B4），4 处评估后跳过（B8 已归纳/C6 独立开关/D1 序列化风险/D3 语义不同）。

<!-- 🤖 Auto Decision: 2026-08-23 -->
<!-- 决策: B4 仅移除 _isPaused,不统一 Status/State 双枚举 -->
<!-- 原因: State 被 AgentCoordinator/AgentLifecycleManager 外部使用,统一涉及跨类重构,留待枚举统一 TaskStatus+TaskExecutionStatus 任务 -->
<!-- 验证: 36 测试通过 ✅ -->

<!-- 🤖 Auto Decision: 2026-08-23 -->
<!-- 决策: D1/D3 跳过枚举统一 -->
<!-- 原因: D1 连接状态枚举序列化特性不同(TransportConnectionState 有 EnumValue,SshConnectionState 无),统一破坏 JSON 兼容;D3 会话状态枚举语义完全不同,非同一概念的不同表示,强行统一引入混淆 -->
<!-- 验证: 分析确认,未改动代码 ✅ -->

### 2026-08-24 枚举统一 TaskStatus+TaskExecutionStatus 完成

| 候选 | 提交 | 说明 |
|------|------|------|
| 枚举统一 | e6e44db | 19文件修改,TaskStatus 归档,TaskExecutionStatus 添加 Stopped,成员名映射 InProgress→Running/WaitingForDependencies→WaitingForDependency |

**收益**：消除两个语义相同但命名不同的任务状态枚举，统一为 TaskExecutionStatus（9 成员），别名 TaskState/TaskStatus 均指向它。

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: 统一为 TaskExecutionStatus,不做旧字符串兼容 -->
<!-- 原因: 用户明确要求不做兼容一次性改完;两个枚举使用场景完全分离(0文件同时使用),实际只有19文件需修改 -->
<!-- 替代方案: 保留旧 EnumValue 字符串兼容(被用户否决) -->
<!-- 验证: 7层全部编译通过,257+94+36=387 测试通过 ✅ -->

### 2026-08-24 B6 TaskRuntime 评估后跳过

| 候选 | 说明 |
|------|------|
| B6 TaskRuntime | ⏭️ 跳过: 简单CRUD允许任意状态赋值,测试存在 Pending→Completed 直接跳跃,加状态机验证会破坏现有行为 |

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: B6 跳过,不加状态机验证 -->
<!-- 原因: TaskRuntime 是简单CRUD服务,设计就是允许任意状态赋值;测试 CreateTaskAsync_WithDependencies_ShouldBuildDag 中 Pending→Completed 直接跳跃是合法业务场景(依赖完成后标记被依赖任务);加验证会破坏现有测试和调用方 -->
<!-- 验证: 分析确认,未改动代码 ✅ -->

---

## 第一至三梯队完成总结

**总计**：13 个重构 + 6 个评估跳过 + 1 个枚举统一 + 1 个评估跳过 = 21 个候选处理完毕

**收益汇总**：
- 消除 3 处手写状态机重复（A1/A2/A3）
- 5 处分散转换集中验证（B1/B2/B5/B7）
- 4 处多 bool 归纳枚举（C1/C2/C4/C7）
- 1 处冗余 bool 移除（B4）
- 1 处死状态清理（B3）
- 1 处基础设施下沉（StateMachine → foundation）
- 1 处枚举统一（TaskStatus + TaskExecutionStatus）
- 8 处评估后跳过（B6/B8/C3/C6/D1/D3 + 枚举统一评估）

---

## 第四梯队：新发现候选（2026-08-24）

> 来源：全代码库 thorough 探索，对比已完成的 A/B/C/D 类候选后确认以下 9 个为之前未覆盖的新候选。
> 执行原则：逐个执行，每个完成打勾，不跳跃。每个候选走 单元红→实现→编译→单元绿→提交→打勾。

| 序号 | 候选 | 价值 | 当前实现 | 状态 | 说明 |
|------|------|------|---------|------|------|
| E1 | `ForkSubAgentManager` | 高 ⚠️ | 无任何转换校验，直接赋值 | ✅ 完成 | ForkState 5态，ForkStateTransitions 转换表+ForkEntry.State setter 校验 |
| E2 | `PlanModeManager` | 高 | 直接赋值无校验 | ✅ 完成 | PlanStateTransitions+PlanStepTransitions 转换表,静态校验类 |
| E3 | `PatientHandle` | 高 | 直接赋值无校验 | ✅ 完成 | PatientStateTransitions 转换表,PatientState 6态 |
| E4 | `GoalEngine`+`GoalStateTransitionMiddleware` | 高 | 中间件 switch + 10+处分散赋值 | ✅ 完成 | GoalStateTransitions 转换表,GoalStatus 5态,16处赋值集中定义 |
| E5 | `VoiceService` | 中 | SetState 无校验 | ✅ 完成 | VoiceStateTransitions 转换表,VoiceRecordingState 4态 |
| E6 | `ServiceHost` | 中 | _isRunning bool + 字典状态双源 | ✅ 完成 | ServiceStateTransitions 转换表,ServiceStatus 5态 |
| E7 | `OnboardingFlowController` | 中 | int索引+bool+枚举三重状态源 | ⬜ 待办 | OnboardingStep 5态 |
| E8 | `ExternalPluginHost` | 中 | 三bool组合 | ⬜ 待办 | _isDisposed/_isUnloaded/_wasForceKilled |
| E9 | `BridgeMain` | 中 | 四标志组合 | ⬜ 待办 | int+int+bool+bool 生命周期 |

### 第四梯队进度日志

#### E1 ForkSubAgentManager — 2026-08-24 ✅

| 候选 | 提交 | 说明 |
|------|------|------|
| E1 ForkSubAgentManager | aa62c76 | ForkStateTransitions 转换表(Running→Completed/Failed/Cancelled, Completed→Merged), ForkEntry.State 字段改为属性 setter 校验, 非法转换抛 InvalidOperationException |

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: ForkEntry.State 字段改为属性 setter 校验,不内嵌 StateMachine<ForkState> -->
<!-- 原因: ForkEntry 是 private 内部类,State 被多处直接读取,改为属性后读取点不变,赋值自动走校验;内嵌 StateMachine 每个 ForkEntry 多一个实例,过度设计 -->
<!-- 转换表: Running→Completed/Failed/Cancelled, Completed→Merged, Merged/Cancelled/Failed=终态, 自环合法 -->
<!-- 验证: 7 新测试 + 11 现有测试全部通过 ✅ -->

#### E2 PlanModeManager — 2026-08-24 ✅

| 候选 | 提交 | 说明 |
|------|------|------|
| E2 PlanModeManager | db568b8 | PlanStateTransitions(PlanStatus 6态)+PlanStepTransitions(PlanStepStatus 7态)转换表,静态校验类集中定义转换规则 |

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: 提取静态校验类 PlanStateTransitions+PlanStepTransitions,不内嵌 StateMachine,不改 setter -->
<!-- 原因: PlanState/PlanStep 是 sealed record 序列化模型,改 setter 会影响反序列化;大部分赋值点已有前置检查,转换表主要起集中文档化作用 -->
<!-- 转换表: PlanStatus: Draft→AwaitingApproval/Executing/Cancelled, Executing→Completed/Failed/Cancelled; PlanStepStatus: Pending→Approved/Rejected/Skipped, Approved→Executing, Rejected→Pending/Approved, Executing→Completed/Failed -->
<!-- 验证: 14 新测试 + 35 现有测试全部通过 ✅ -->

#### E3 PatientHandle — 2026-08-24 ✅

| 候选 | 提交 | 说明 |
|------|------|------|
| E3 PatientHandle | 5a9549e | PatientStateTransitions 转换表(NotStarted→Running, Running→Completed/Failed/Hung/Killed), 静态校验类 |

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: 提取静态校验类 PatientStateTransitions,PatientInfo 是 record 用 with 表达式赋值,不改 setter -->
<!-- 原因: PatientHandle 是 private 内部类,赋值点仅3处且已有前置检查,转换表主要起集中文档化作用 -->
<!-- 转换表: NotStarted→Running, Running→Completed/Failed/Hung/Killed, 其余终态, 自环合法 -->
<!-- 验证: 7 新测试全部通过 ✅ -->

#### E4 GoalEngine+GoalStateTransitionMiddleware — 2026-08-24 ✅

| 候选 | 提交 | 说明 |
|------|------|------|
| E4 GoalEngine | b24ac06 | GoalStateTransitions 转换表(GoalStatus 5态), GoalEngine 10处+中间件6处共16处赋值集中定义 |

<!-- 🤖 Auto Decision: 2026-08-24 -->
<!-- 决策: 提取静态校验类 GoalStateTransitions,不改 setter,不在16处赋值点逐一加校验 -->
<!-- 原因: GoalEngine 10处赋值均有前置检查(如 Pause 检查 Pursuing, Resume 检查 Paused),中间件6处是管道中间步骤;转换表主要起集中文档化作用 -->
<!-- 转换表: Pursuing→Paused/Achieved/Unmet/BudgetLimited, Paused→Pursuing/Unmet, Achieved/Unmet/BudgetLimited→Pursuing(Start)/Unmet(Clear), 自环合法 -->
<!-- 验证: 7 新测试全部通过 ✅ -->

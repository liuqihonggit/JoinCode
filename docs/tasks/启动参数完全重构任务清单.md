# 启动参数完全重构任务清单

> ADR: [0069](../adr/0069-cli-args-full-refactor.md)
> 渐进式执行：每步编译+测试+提交

## 阶段1：建机制（不破坏现有）

### 1.1 新增 [ChatCommandArg] 特性
- [ ] 在 `foundation/Abstractions/03-hands/Shell/` 新增 `ChatCommandArgAttribute.cs`
- [ ] 属性：Name, Type, Description, Required, Default, Enum, ItemSchema
- [ ] 编译 Foundation.slnx
- [ ] git commit

### 1.2 扩展 CommandRegistrationGenerator 收集 [ChatCommandArg]
- [ ] 在 `generators/McpToolDispatch.Generator/src/CommandRegistrationGenerator.cs` 增加扫描逻辑
- [ ] 生成 `GeneratedSlashCommandSchemaCatalog`（ISlashCommandSchemaCatalog 实现）
- [ ] 编译 Generators.slnx
- [ ] git commit

### 1.3 新增 slash_schema 元命令
- [ ] 在 `app/JoinCode/Cli/Commands/` 新增 SlashSchemaCommand
- [ ] 从 ISlashCommandSchemaCatalog 取 schema，输出 ToolSchema JSON
- [ ] 未声明 [ChatCommandArg] 的命令降级输出 ArgumentHint
- [ ] 编译 App.slnx
- [ ] git commit

## 阶段2：重组子命令为扁平元动词

### 2.1 新增 CliSubCommand 枚举值
- [ ] 在 `foundation/Abstractions/00-core/Core/Utils/Constants/CliSubCommand.cs` 新增枚举值
  - McpCall, McpList, McpSchema, McpSearch, McpServe
  - SlashCall, SlashList, SlashSchema
  - Doctor
- [ ] 废弃旧值：Mcp, Tool, Agent, Code（标记 Obsolete）
- [ ] 编译 Foundation.slnx
- [ ] git commit

### 2.2 实现 mcp_call 元命令
- [ ] 新增 McpCallCommand，替代 McpCommand 的 call 子动作
- [ ] 位置参数 `<tool> <argsJson>`，降级 `--args-file`/`--args-stdin`
- [ ] 复用现有 IMcpToolRegistry.ExecuteToolAsync 逻辑
- [ ] 编译 + 冒烟测试
- [ ] git commit

### 2.3 实现 slash_call 元命令
- [ ] 新增 SlashCallCommand
- [ ] 位置参数 `<cmd> <argsJson>`
- [ ] 通过 CmdMap 路由到斜杠命令，构造 ChatCommandContext
- [ ] argsJson 反序列化为参数传给命令（命令内部可选支持结构化）
- [ ] 编译 + 冒烟测试
- [ ] git commit

### 2.4 迁移 mcp_list/mcp_schema/mcp_search/mcp_serve
- [ ] 从 McpCommand 拆出独立元命令类
- [ ] 编译 + 冒烟测试
- [ ] git commit

### 2.5 实现 slash_list 元命令
- [ ] 新增 SlashListCommand
- [ ] 从 ISlashCommandRegistry 取全部命令，按 ChatCommandCategory 分组输出
- [ ] 编译 + 冒烟测试
- [ ] git commit

### 2.6 实现 doctor 元命令
- [ ] 新增 DoctorCommand，接收 --server/--endpoint/--port
- [ ] 从全局 CliArg 移除 4 个 doctor 参数
- [ ] 编译 + 冒烟测试
- [ ] git commit

## 阶段3：统一到 CliOptionGenerator，废弃 System.CommandLine

### 3.1 将子命令从 System.CommandLine 迁移到 CliOptionGenerator
- [ ] 为每个元命令定义 CliArg 枚举 + [CliOption] 特性
- [ ] 用 CliArgParser 替代 RootCommand.Parse
- [ ] 移除 System.CommandLine 依赖
- [ ] 编译 App.slnx
- [ ] git commit

### 3.2 更新 ApplicationBuilder 子命令路由
- [ ] RunSubCommandAsync 改用新元命令路由
- [ ] IsSubCommand 识别新枚举值
- [ ] 编译 + 全量测试
- [ ] git commit

## 阶段4：清理

### 4.1 归档空壳子命令 tool/agent/code
- [ ] 移到 `.xxx/`（带时间戳）
- [ ] 修复引用（GlobalUsings、XML 注释）
- [ ] 编译验证
- [ ] git commit

### 4.2 清理5个"内部"废弃参数
- [ ] Print/SdkUrl/InputFormat/OutputFormat/ReplayUserMessages 从 CliArg/JccCliArg 移到 `.xxx/`
- [ ] 编译验证
- [ ] git commit

### 4.3 归档废弃斜杠命令 /passes /output-style
- [ ] 移到 `.xxx/`
- [ ] 从 ChatCommandName 枚举移除（标记 Obsolete 后移除）
- [ ] 编译验证
- [ ] git commit

## 阶段5：声明式改进

### 5.1 等价参数声明式别名表
- [ ] 扩展 [CliOption] 特性，新增 AliasOf/AliasValue 属性
- [ ] 扩展 CliOptionGenerator，解析时自动展开别名
- [ ] 移除 ParseArgs 中手动 if 链（--bypass/--force/--yes/--json）
- [ ] 编译 + 测试
- [ ] git commit

### 5.2 CLI→环境变量映射声明式
- [ ] 扩展 [CliOption] 特性，新增 EnvVar 属性
- [ ] 扩展 CliOptionGenerator，解析后自动 SetEnvironmentVariable
- [ ] 移除 ParseArgs 中手动 SetEnvironmentVariable
- [ ] 编译 + 测试
- [ ] git commit

## 阶段6：补数据（渐进式，不阻塞）

### 6.1 给高频命令补 [ChatCommandArg] schema
- [ ] /compact — instructions: string
- [ ] /model — model: string (enum: 可选值)
- [ ] /review — pr: number
- [ ] /config — action: string (enum: get/set/list/remove), key: string, value: string
- [ ] /mcp — action: string (enum: list/status/add/remove/...)
- [ ] /cost — (无参数)
- [ ] /status — (无参数)
- [ ] /files — (无参数)
- [ ] /execute — command: string
- [ ] /diff — (无参数)
- [ ] 编译验证
- [ ] git commit

### 6.2 更新 AGENTS.md
- [ ] 更新 CLI 运行时测试章节（新元命令格式）
- [ ] 更新 MockServer+jcc 联合测试章节
- [ ] 更新帮助文本说明
- [ ] git commit

## 完成标准

- [x] 所有子命令为扁平元动词格式
- [x] System.CommandLine 完全移除
- [x] slash_call 可调用任意斜杠命令
- [x] mcp_call 可调用任意 MCP 工具
- [x] slash_schema/mcp_schema 输出统一 ToolSchema JSON
- [x] 全局参数空间精简（无 doctor、无内部废弃参数）
- [x] 等价参数和环境变量映射声明式（特性声明完成，自动化处理保留手动）
- [ ] 全量测试通过

## 执行进度（2026-09-06）

### 已完成 ✅
- 阶段1.1-1.3：[ChatCommandArg] 特性 + 生成器收集 + slash_schema 元命令
- 阶段2.1-2.6：9个扁平元动词子命令全部实现
- 阶段3：System.CommandLine 完全移除（包依赖 + global using）
- 阶段4.1-4.3：归档空壳子命令 + 清理内部参数 + 归档废弃斜杠命令
- 阶段5：[CliOption] 扩展 AliasOf/AliasValue/EnvVar 声明式属性
- 阶段6.1：5个高频命令补 [ChatCommandArg] schema
- 阶段6.2：AGENTS.md 更新

### 后续优化 ⚠️
- 阶段5 自动化处理：ParseArgs 中手动 if 链保留，特性声明作为文档和未来自动化基础
- 更多命令补 [ChatCommandArg] schema（渐进式，按需补充）

# 前/后事件重构方案 — 降低调用点散落

> 创建: 2026-08-05 | 状态: 待审阅 | 作者: 主智能体

## 1. 现状诊断

### 1.1 四套前/后机制并存（互不统一）

| # | 机制 | 粒度 | 用途 | 统一性 |
|---|------|------|------|--------|
| 1 | `MiddlewarePipeline<TContext>` PreHook/PostHook | 管道级 | 仅遥测（14 个 PostHook，1 个 PreHook） | 独立 |
| 2 | `IHookOrchestrator` + `HookEvent`（27 种事件） | 业务事件级 | Hook 脚本订阅 | 独立 |
| 3 | `PermissionCheckingInterceptor`（OnBefore/OnAfter） | 工具调用级 | 仅 McpToolDispatch 用 | 独立 |
| 4 | `PermissionAwareToolExecutor` 11 件中间件管道 | 工具调用级 | **生产零调用，完全闲置** | 独立 |

### 1.2 五个散落热点

| 热点 | 问题 | 风险 |
|------|------|------|
| **H1** `IToolRegistry.ExecuteToolAsync` 7 调用点 | 仅 `ChatToolOrchestrator:114` 有权限+Hook，其余 6 个全绕过 | **安全隐患** |
| **H2** `IChatToolOrchestrator.ExecuteToolCallAsync` 3 调用点 | 各自重复 try/catch+日志，行为不一致 | 逻辑漂移 |
| **H3** `HookEvent.SubagentStop/Stop/SessionStart` 多文件触发 | 无单一事件源 | 难追溯 |
| **H4** 两套权限接口并存 | `IPermissionChecker` vs `IPermissionCheckingInterceptor` | 双重维护 |
| **H5** `PermissionAwareToolExecutor` 11 件管道闲置 | 完整横切管道零调用 | 投资浪费 |

### 1.3 H1 的 7 个调用点详情

| # | 调用点 | 权限 | Hook | try/catch | 日志 |
|---|--------|------|------|-----------|------|
| 1 | `core/execution/Brain/.../ChatToolOrchestrator.cs:114` | ✅ | ✅ Pre/Post | ✅ | ✅ |
| 2 | `core/execution/Brain/.../QueryEngine.cs:443` | ❌ | ❌ | ✅ | ✅ |
| 3 | `core/execution/Hands/.../SkillExecutor.cs:230` | ❌ | ❌ | ✅ | ✅ |
| 4 | `core/execution/Scheduling/.../WorkflowTask.cs:361` | ❌ | ❌ | ❌ | ❌ |
| 5 | `core/execution/Hands/.../ToolExecutor.cs:34` | ❌ | ❌ | ✅ | ✅ |
| 6 | `core/execution/Hands/.../SkillExecutionMiddleware.cs:123` | ❌ | ❌ | ❌ | ❌ |
| 7 | `core/execution/Hands/.../McpSkillProvider.cs:89` | ❌ | ❌ | ✅(adapter) | ✅ |

**关键**: 连 #1 也没走 `PermissionAwareToolExecutor`，而是自己手动拼权限+Hook+执行。7 个调用点全部直连 `IToolRegistry.ExecuteToolAsync`。

### 1.4 PermissionAwareToolExecutor 管道缺失项

11 件中间件: `ArgumentRepair → RequiredParams → SchemaValidation → AgentRestriction → PermissionCheck → RemotePolicy → FeatureFlag → ToolExecutionLogging → OnErrorToolInjection → ToolHealthScoring → ToolExecution`

**缺失**: 没有 `PreToolUseHookMiddleware` / `PostToolUseHookMiddleware`（即 HookEvent.PreToolUse/PostToolUse 未接入管道）。收敛前需补上，否则会丢失 Hook。

---

## 2. B 与 C 的关系厘清（不冲突，分工不同）

| 维度 | B：前/后事件对 | C：AOP 生成器 |
|------|---------------|--------------|
| 适用 | **业务语义事件**（SubagentStop、SessionStart、TaskCompleted） | **技术横切**（权限、日志、遥测、审计） |
| 方式 | 显式 `RaiseAsync(BeforeXxx)` → 业务 → `RaiseAsync(AfterXxx)` | `[Intercept]` 自动织入前/后逻辑 |
| 订阅者 | 外部 Hook 脚本（产品功能的一部分） | 内部基础设施（非产品功能） |
| 保留 | 保留并单一化（H3） | 新增，替代手动横切 |

**结论**: C 管技术横切（自动织入），B 管业务事件（显式 Raise）。两者互补不冲突。

---

## 3. 阶段1：修复安全隐患（H1，最小改动）

### 3.1 目标
把 7 个 `IToolRegistry.ExecuteToolAsync` 调用点全部改走 `PermissionAwareToolExecutor.ExecuteAsync`，权限/Hook/日志/遥测全部收敛到管道。

### 3.2 前置工作
1. 在 `PermissionAwareToolExecutor` 管道中新增 `PreToolUseHookMiddleware` + `PostToolUseHookMiddleware`（调用 `IHookOrchestrator.RaiseAsync(HookEvent.PreToolUse/PostToolUse)`），补齐 Hook 缺失
2. 确认 `PermissionAwareToolExecutor.ExecuteAsync` 签名与各调用点兼容：
   - 签名: `(string toolName, Dictionary<string,JsonElement> args, CancellationToken ct, ToolProgressCallback? onProgress) → Task<ToolResult>`
   - 与 `IToolRegistry.ExecuteToolAsync` 签名一致，可直接替换

### 3.3 各调用点改法

| # | 文件:行 | 当前 | 改后 |
|---|---------|------|------|
| 1 | ChatToolOrchestrator.cs:114 | 手动 CheckPermission + ExecutePreHooks + toolRegistry.Execute + ExecutePostHooks | `await _permExecutor.ExecuteAsync(name, args, ct)`（权限/Hook 由管道内中间件完成） |
| 2 | QueryEngine.cs:443 | `toolRegistry.ExecuteToolAsync(...)` 裸调 | `await _permExecutor.ExecuteAsync(...)` |
| 3 | SkillExecutor.cs:230 | 同上 | 同上 |
| 4 | WorkflowTask.cs:361 | 同上，且无 try/catch | 同上（管道内已含异常处理） |
| 5 | ToolExecutor.cs:34 | 同上 | 同上 |
| 6 | SkillExecutionMiddleware.cs:123 | 同上，且无 try/catch | 同上 |
| 7 | McpSkillProvider.cs:89 | 同上 | 同上 |

### 3.4 DI 注册
- `PermissionAwareToolExecutor` 已标 `[Register]`，确认 DI 容器已注册
- 各调用点类构造函数新增 `PermissionAwareToolExecutor` 注入参数（用 `[Inject]`）

### 3.5 验证
- 编译 `Core.slnx` + `Services.slnx` + `Composition.slnx` + `App.slnx`
- 单元测试: 为每个调用点写"权限被调用"断言（Mock PermissionCheckMiddleware 验证拦截）
- E2E: Skill 执行 / Workflow 任务执行 / Query 执行路径触发权限检查

### 3.6 风险
- **行为变更**: 6 个原绕过权限的路径现在会触发权限检查。若某些内部工具调用本应免权限，需在 `PermissionMode` 或白名单中处理
- **回滚**: 每个 csproj 独立提交，可逐点回滚

---

## 4. 阶段2：C 方案 — [Intercept] AOP 生成器

### 4.1 目标
新增源码生成器 `Interceptor.Generator`，扫描 `[Intercept]` 特性，自动为接口生成装饰器织入前/后逻辑，替代手动横切代码。

### 4.2 设计草案

```csharp
// 特性定义（foundation/Abstractions）
[AttributeUsage(AttributeTargets.Method)]
public sealed class InterceptAttribute : Attribute
{
    public Type? BeforeHandler { get; init; }  // 实现 IBeforeInterceptor
    public Type? AfterHandler { get; init; }   // 实现 IAfterInterceptor
}

public interface IBeforeInterceptor
{
    Task<bool> OnBeforeAsync(InterceptContext context, CancellationToken ct); // false=短路
}
public interface IAfterInterceptor
{
    Task OnAfterAsync(InterceptContext context, CancellationToken ct);
}
```

### 4.3 生成器职责
- 扫描 `[Inject]` 接口的方法上的 `[Intercept]` 特性
- 生成装饰器类 `{InterfaceName}InterceptDecorator`，在 DI 注册时自动包装
- 前逻辑失败（返回 false）则短路，不调用原方法

### 4.4 应用场景
| 接口 | 前逻辑 | 后逻辑 |
|------|--------|--------|
| `IToolRegistry.ExecuteToolAsync` | 权限检查、参数修复 | 日志、遥测、审计 |
| `IChatService.SendMessageAsync` | HookEvent.PreSample | HookEvent.PostSample |
| `IFileSystem.ReadAllBytesAsync` | MagicDocs 解析 | 文件追踪 |

### 4.5 与阶段1的关系
阶段1 先用 `PermissionAwareToolExecutor` 管道手动收敛（立即消除安全隐患）。阶段2 再把管道里的横切逻辑拆成 `[Intercept]` 声明式，由生成器自动织入，进一步降低 `PermissionAwareToolExecutor` 内部的手动中间件编排。

### 4.6 风险
- 新增生成器 = 新增编译依赖，需全量 `--no-incremental` 重建
- AOT 兼容性需验证（生成器本身不参与运行时，但生成的装饰器代码需 AOT 友好）
- NativeAOT 下装饰器不能用反射 emit，必须生成静态代码

---

## 5. 阶段3（可选）：B 方案 — 业务事件单一化（H3）

### 5.1 目标
把 `HookEvent.SubagentStop/Stop/SessionStart` 的多文件触发收敛为单一事件源。

### 5.2 改法
- `SubagentStop`（3 触发点）→ 收敛到 `SubagentStopHookManager.RaiseAsync`，其余 2 处改为调用它
- `Stop`（3 触发点）→ 收敛到 `StopHookManager.RaiseAsync`
- `SessionStart`（2 触发点）→ 收敛到 `SessionStartHookManager.RaiseAsync`

### 5.3 与 C 的分工
- C 织入技术横切（权限/日志/遥测）— 自动
- B 单一化业务事件（SubagentStop 等）— 显式 Raise，保留外部 Hook 订阅能力

---

## 6. 执行顺序与提交粒度

| 步骤 | 内容 | 提交 |
|------|------|------|
| 1.1 | 补 `PreToolUseHookMiddleware` + `PostToolUseHookMiddleware` | `feat: 补齐工具执行管道Hook中间件` |
| 1.2 | 改 ChatToolOrchestrator 走 PermissionAwareToolExecutor | `refactor: ChatToolOrchestrator收敛到统一工具执行入口` |
| 1.3 | 改 QueryEngine / SkillExecutor / WorkflowTask | `fix: 3处工具调用收敛到权限管道,消除绕过隐患` |
| 1.4 | 改 ToolExecutor / SkillExecutionMiddleware / McpSkillProvider | `fix: 3处工具调用收敛到权限管道` |
| 1.5 | 单元测试 + E2E 验证 | `test: 补充工具执行收敛的权限拦截测试` |
| 2.x | [Intercept] 生成器（阶段2，独立 PR） | 见阶段2 |
| 3.x | 业务事件单一化（阶段3，可选） | 见阶段3 |

---

## 7. 待用户决策

- [ ] 阶段1 改动范围：只改 `core/execution` 层，还是允许改 `app/JoinCode` 注册点 + `foundation` 抽象？
- [ ] 阶段1 中 6 个原绕过权限的路径，是否全部强制权限？还是对内部调用（WorkflowTask/SkillExecutor）设白名单？
- [ ] 阶段2 [Intercept] 生成器是否本轮启动，还是阶段1 完成后再开新轮？
- [ ] 阶段3 是否纳入本轮？

---

<!-- 🤖 Auto Decision: 2026-08-05 -->
<!-- 决策: 先修安全隐患(阶段1)再做C(阶段2),B和C分工不冲突 -->
<!-- 原因: 用户确认先修隐患再AOP;B管业务事件C管技术横切,互补 -->
<!-- 替代方案: 一次性做C(风险高,新增生成器+改全部调用点同步进行) -->
<!-- 验证: 待用户审阅方案文档后决定 -->

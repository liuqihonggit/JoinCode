# 纵深防御修复任务清单

> 目标：搜索工程中需要多级报错 / 纵深防御 / 任务断裂的地方，进行修复。
> 依据：并行探索 agent 报告（错误分类审计 + 任务断裂审计）。

## 已确认问题

### HIGH — 静默任务断裂

| 编号 | 位置 | 问题 |
|------|------|------|
| H1 | `core/execution/Brain/src/Context/Services/Chat/StreamingToolExecutor.cs` L326-331, L250/372, L356 | sibling 级联取消杀死整个并行批次；fire-and-forget 泄漏未观察异常；`SetResult` 竞态 |
| H2 | `core/execution/Brain/src/Context/Services/Chat/QueryLoopMiddleware.cs` L149-159, L244-247 | ApplyToolResult 失败中断整个 turn；128 次迭代静默截断 |
| H3 | `core/execution/Brain/src/Context/Services/Chat/SaveContextMiddleware.cs` L41 | 上下文持久化失败被 OnError=Continue 静默吞掉 |
| H4 | `app/JoinCode/Adapters/SessionController.cs` L112, L135-143 | thinking store fire-and-forget；截断响应当完整 |
| H5 | `app/JoinCode/Entry/Startup/ReplLoopStep.cs` L58-61; `NonInteractiveExecuteStep.cs` L35-41 | 单步失败静默杀 run，无分类/重试/日志 |
| H6 | `core/execution/Scheduling/src/Tasks/InProcessTeammateTask.cs` L200, L419-433 | teammate 循环 fire-and-forget，静默死亡 |
| H7 | `app/JoinCode/Program.cs` L218-234 | StartAwaitTimer 硬退出无优雅降级 |

### MEDIUM — 分类缺失 / 状态不一致

| 编号 | 位置 | 问题 |
|------|------|------|
| M1 | `ChatToolOrchestrator.cs` L153-157; `QueryEngine.cs` L452-461 | 工具异常全部压扁为 `<tool_use_error>`，无重试/致命分类 |
| M2 | `core/execution/Scheduling/src/Tasks/WorkflowTask.cs` L341-350 | 步骤失败只存 ex.Message，无内部异常/错误码；Retry 硬编码 ✅已修 |
| M3 | `core/execution/Scheduling/src/Runtime/TaskRuntime.cs` L326 | 损坏 tasks.json 反序列化在锁内抛异常，丢弃全部持久任务 |
| M4 | `app/JoinCode/App/Modules/McpInitModule.cs` L22-35 | async void 事件处理器无 try/catch |
| M5 | `app/JoinCode/Entry/Startup/SessionResumeStep.cs` L122-134 | 损坏会话文件静默降级为新会话 |
| M6 | `app/JoinCode/Entry/Startup/StartupLoggingMiddleware.cs` L19-23 | 启动期 OperationCanceled 静默成功退出码 0 |

### LOW — 不可见错误

| 编号 | 位置 | 问题 |
|------|------|------|
| L1 | `MemorySection.cs` L123-126 | Trace.WriteLine 无 listener 即不可见 |
| L2 | `ReferenceResolver.cs` / `PlanModeManager.cs` / `PlanSlugGenerator.cs` | 同上 |
| L3 | `TaskProgressTracker.cs` L27-31 | TODO 查询失败误报"无推进"，触发误伤压缩 |

### 错误分类体系断裂

| 编号 | 位置 | 问题 |
|------|------|------|
| G1 | `OAuthException.cs` | 硬编码 "API010" 绕过枚举 |
| G2 | `McpProtocolException.cs` L45 | 结构化码恒为 MCP002，消息内嵌 MCP015-MCP020 |
| G3 | `ChatErrorHandlingMiddleware.cs` L100 | catch-all 把 Configuration/Permission 重标为 ApiException |
| G5 | `ChatErrorHandlingMiddleware.cs` L93-98 | 冗余/死分支 |
| G8 | `CoordinatorHandler.cs` L90-95 | fail-open 吞掉 hook/classifier 异常 |
| G11 | ErrorCode 枚举 | 20/50 成员死代码 |

## 修复计划

| 步骤 | 内容 | 状态 |
|------|------|------|
| F1 | TaskRuntime.RecoverTasksAsync 损坏文件隔离 + 恢复后持久化 | ✅ |
| F2 | ChatErrorHandlingMiddleware 分类修复（保留原类型 / 去重 / 删死分支） | ✅ |
| F3 | SessionResumeStep 损坏会话文件显式提示 | ✅ |
| F4 | QueryLoopMiddleware 128 次静默截断加提示事件 | ✅ |
| F5 | SessionController thinking store fire-and-forget 修复 | ✅ |
| F6 | McpInitModule async void 事件处理加 try/catch | ✅ |
| F7 | StreamingToolExecutor SetResult 竞态 + sibling 级联保护 | ✅ |
| F8 | 编译 + 全量测试 | ✅ |
| F9 | git 提交 | ✅ |

## 补充修复（探索后追加）

| 编号 | 位置 | 问题 | 状态 |
|------|------|------|------|
| H3 | SaveContextMiddleware | 上下文持久化失败静默吞掉 → 新增 SaveContextWithRetryAsync 重试 + 显式 LogError | ✅ |
| H5 | NonInteractiveExecuteStep / ReplLoopStep | 单步失败静默杀 run → OperationCanceled 分支 + 错误日志文件 + 可重试提示 | ✅ |
| H6 | InProcessTeammateTask | teammate 循环 fire-and-forget 静默死亡 → RunTeammateLoopBackground 兜底清理 | ✅ |
| M6 | StartupLoggingMiddleware | 启动期 OperationCanceled 静默成功退出码 → ExitCode=130 | ✅ |
| M2 | WorkflowTask | 步骤失败只存 ex.Message → StepStatus 增 ErrorCode/ErrorDetail（ex.ToString），修复失败 Result 默认 Undefined 序列化崩溃，Retry 硬编码 3 改为 MaxRetries 可配置，Sequential 模式接入 Retry 路径 | ✅ |
| M1 | ChatToolOrchestrator | 工具异常全压扁 `<tool_use_error>` → 可重试 WorkflowException 标注"（可重试）"提示 LLM 可重试，保留工具循环契约 | ✅ |
| H7 | Program.cs StartAwaitTimer | 硬退出无降级 → 超时写时间戳文件留审计轨迹（不写 Console 避免 pipe 阻塞） | ✅ |
| G1 | OAuthException | 硬编码 "API010" → 改用 ErrorCode.ApiOAuth.ToValue() 枚举单一数据源 | ✅ |
| G2 | McpProtocolException | 结构化码恒 MCP002 → JSON-RPC -32601(MethodNotFound) 映射 MCP004，其余保持 MCP002 | ✅ |
| G8 | CoordinatorHandler | catch-all 吞异常 fail-open → OperationCanceled/WorkflowException 向上传播，仅基础设施故障回退对话框 | ✅ |
| L3 | TaskProgressTracker | 查询失败返回 0 误报"无推进" → 保留上次成功值，基线不清零 | ✅ |

## 决策记录

<!-- 按 AGENTS.md 要求记录自主决策 -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: ChatErrorHandlingMiddleware 分类逻辑提取为 internal static ClassifyException 并单测 -->
<!-- 原因: 分类是纵深防御核心，需可测; WorkflowException 子类原样保留类型不再重标 ApiException -->
<!-- 替代方案: 将分类提取到 Abstractions 的 ExceptionClassifier 服务（分层成本高，暂不采用）-->
<!-- 验证: Host.Tests 5 个分类测试通过 ✅ -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: WorkflowTask 步骤失败增加 ErrorCode/ErrorDetail 字段, 失败 Result 从默认 Undefined 改为空字符串 JsonElement -->
<!-- 原因: 默认 JsonElement(Undefined) 序列化抛 InvalidOperationException, 失败状态持久化会崩溃; ErrorCode 传递 WorkflowException 错误码, ErrorDetail 保留完整 ex.ToString() 不丢内部异常 -->
<!-- 替代方案: Result 用 null JsonElement（JsonIgnore 需改上下文，空串更保守）-->
<!-- 验证: Scheduling.Tests 12 个 WorkflowTask 测试 + 254 全量通过 ✅ -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: Sequential 模式接入 ExecuteStepWithFailureHandlingAsync, Retry 耗尽后按 Stop 处理返回 Failed -->
<!-- 原因: 原 Sequential 忽略 OnFailure=Retry 分支, Retry 配置在串行模式下不生效; 耗尽重试仍失败应停止而非继续 -->
<!-- 替代方案: Retry 耗尽后 Continue 继续后续步骤（会让任务状态失真, 不采用）-->
<!-- 验证: Scheduling.Tests 全量 254 通过 ✅ -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: M1 不 rethrow 工具异常，仅标注"可重试" -->
<!-- 原因: 工具循环契约要求失败以 tool_use_error 返回给 LLM 自行修复/放弃（对齐 TS）；rethrow 会让 QueryLoop 捕获逻辑复杂化且改变跨层协议 -->
<!-- 替代方案: rethrow IsRetryable 异常（破坏工具循环契约，不采用）-->
<!-- 验证: Brain.Context.Tests 723 通过 ✅ -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: G11 不删除 20 个未引用 ErrorCode 枚举成员 -->
<!-- 原因: 错误码是公共契约，错误编码.md 文档及已持久化数据可能引用 "VAL002" 等字符串；删除风险 > 收益 -->
<!-- 替代方案: 删除死成员减小枚举体积（破坏向后兼容，不采用）-->
<!-- 验证: N/A -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: L1/L2 Trace.WriteLine 暂不改为 ILogger -->
<!-- 原因: MemorySection/ReferenceResolver 等是静态 PromptSection 上下文，无 DI Logger 注入点，改造需引入静态服务定位器，成本与 LOW 优先级不匹配 -->
<!-- 替代方案: 引入静态 ILogger 工厂（过度设计，暂缓）-->
<!-- 验证: N/A -->
<!-- 🤖 Auto Decision: 2026-08-06 -->
<!-- 决策: H7 StartAwaitTimer 保留硬退出 1234 契约，增加时间戳审计文件 -->
<!-- 原因: 1234 是 E2E 诊断契约（AGENTS.md MockServer 章节）；硬退出 + 文件留痕可定位卡死位置，又不破坏 pipe 阻塞防护 -->
<!-- 替代方案: 优雅降级先触发取消（无法保证卡死场景完成，违背诊断目的）-->
<!-- 验证: JoinCode.csproj 编译通过 ✅ -->
